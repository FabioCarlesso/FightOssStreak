package dev.fos.model;

/**
 * Assunto do feedback (docs/13-feedback-usuarios.md) — o dono prioriza por aqui sem abrir cada
 * item.
 */
public enum FeedbackCategory {
    BUG,
    CONTEUDO_ERRADO,
    TROCA_DE_VIDEO,
    SUGESTAO_FUNCIONALIDADE,
    OUTRO
}
